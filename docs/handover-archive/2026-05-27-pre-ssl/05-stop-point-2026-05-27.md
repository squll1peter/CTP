# Stop-Point Handover (2026-05-27)

## What Is Complete

1. Stage timing instrumentation is active and visible in Status output.
2. Runner startup null-config crash path is hardened.
3. Directory import poller no longer fails on null directory listings.
4. DICOM anonymizer now caches script/lookup properties at stage level and
   refreshes only on file change.
5. Packaging and runtime checks succeeded (`ant clean test`, `ant all`).

## Profiling Interpretation (Latest Shared Run)

1. High-cost chain is decompressor + IDMap + anonymizer.
2. AV/file system scanning on Windows is a likely multiplier on all file-heavy
   stages.
3. Filter rejects the majority of incoming objects before decompressor path.

## Confirmed Decisions

1. Decompressor optimization is deprioritized because decompressor is expected
   to be removed from the active pipeline.
2. No multithreaded anonymizer rollout in this batch.

## Next Session First Steps

1. Remove/disable decompressor in active config and run the same workload.
2. Capture fresh stage profile and compare anonymizer + IDMap + storage costs.
3. If IDMap remains high, evaluate commit batching and context reuse in IDMap.
4. If anonymizer remains high, extend stage-level caching approach to adjacent
   script-driven stages where safe.

## Risks/Notes

1. Export ordering constraints still apply if later introducing parallelism.
2. `IntegerTable`/JDBM semantics remain a concurrency guardrail.
3. Keep test gate mandatory on every performance batch.
